# Stash
A Real-Time, ACID-Compliant Peer-to-Peer (P2P) Ledger &amp; Wallet System. P2P ledger system handling concurrent transaction requests using PostgreSQL row-level locking (FOR UPDATE) to completely eliminate double-spending risks.
